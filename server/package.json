{
  "name": "kadr-server",
  "version": "0.1.0",
  "description": "Kadr — self-hosted photo & video backup server",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  }
}
